# Descripteur d'instance : tout ce qui décrit OÙ on déploie, et rien d'autre.
# Le chart applicatif ne connaît ni ce cluster, ni ce namespace — c'est ici que
# l'information vit, et nulle part ailleurs.
#
# Un dossier sans env.yaml est ignoré par le rendu : pas d'état à moitié posé.
cluster:
  context: ovh-dev
  id: c-m-97jxtvnv
namespace: mesure-impact-prod
rancherProjectId: c-m-97jxtvnv:p-bxkq6
